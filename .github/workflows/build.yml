name: Build TID3

on:
  push:
    branches: [ master, dev ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Set build number
      run: echo "BUILD_COUNTER=${{ github.run_number }}" >> $env:GITHUB_ENV

    - name: Restore dependencies
      run: dotnet restore TID3.csproj --runtime win-x64

    - name: Build application
      run: dotnet build TID3.csproj --configuration Release --runtime win-x64 --no-restore
      env:
        BUILD_COUNTER: ${{ github.run_number }}

    - name: Publish Self-Contained Executable
      run: dotnet publish TID3.csproj --configuration Release --runtime win-x64 --self-contained true --output ./publish
      env:
        BUILD_COUNTER: ${{ github.run_number }}

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: TID3-Release-${{ github.run_number }}
        path: ./publish/
        retention-days: 30

    - name: Create Release (on tags)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: ./publish/TID3.exe
        name: TID3 ${{ github.ref_name }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
